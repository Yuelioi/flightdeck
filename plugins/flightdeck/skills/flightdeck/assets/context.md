# {Work title} context

{Stable goal-specific facts, constraints, accepted decisions, and terms that a fresh session would
otherwise have to rediscover. Add descriptive headings only when the content needs them; do not put
live progress here.}
