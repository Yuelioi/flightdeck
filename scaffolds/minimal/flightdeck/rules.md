---
version: 3.0
disabled_folders: []     # the one structured toggle: listed folders never suggested / not flagged as orphans
---

## House rules

<!-- Deck-local flightdeck conventions + behavioral overrides. rules.md is mandatory — do not delete it.
     Defaults are full-auto (every ritual self-invocable, status_auto on, commit confirm, git/emit inferred).
     Tune via ### Autonomy overrides below with standard phrases, not toggles. -->

### Project conventions

### Autonomy overrides
<!-- Omit = defaults. e.g. uncomment: commit without asking  /  landing: don't self-invoke; I run it manually -->
