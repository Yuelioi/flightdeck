# Briefing — notes-cli

## Conventions

Tests live in `tests/`; run `pytest -q` and make it pass before every commit.
Public-facing strings (CLI help, error messages) stay in English.
Ask before force-pushing or rewriting already-published history.

## Subscriptions

<!-- one ~/.flightdeck-relative path per line; empty = subscribe to nothing global -->
<!-- e.g.  knowledge/commit-style.md   (a copy shared across your other decks) -->
