---
status: accepted
---

# Next links drive immediate recovery

Flightdeck does not maintain a separate `Read now` routing section. Next is an executable sentence
containing at most three local links required for that action, while context and an existing Plan
load automatically and all other supporting links remain lazy; this keeps the recovery path in
human-readable prose instead of duplicating it as metadata.
